package cn.tedu.demo;

public class Dog extends Animal{
	//颜色，体重，身高，eat();run();sleep();
	

	public static void main(String[] args) {
		Dog dog1=new Dog();
		System.out.println(dog1.color);
		dog1.eat();
		//定义一个Person类，属性：name，sex,age,height,weight  run();sleep();stduy()
		//定义一个Student类，属性：name，sex,age,height,weight run();sleep();stduy(),play();
		//定义一个Teacher类，属性：name，sex,age,height,subject,weight run();sleep();stduy()
		//定义一个测试类Test,用于创建Student类对象，获取它所具有的属性和行为；
		//用于创建Teacher类对象，获取它所具有的属性和行为
	}

}
